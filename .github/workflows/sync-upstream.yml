name: Sync Upstream / Main

on:
  schedule:
    # 05:00 Asia/Shanghai = 21:00 UTC (previous day)
    - cron: "0 21 * * *"
  workflow_dispatch:
    inputs:
      target_branch:
        description: "Target branch in this repository"
        required: false
        default: "main"
        type: string
      upstream_repo:
        description: "Upstream repository in owner/name format"
        required: false
        default: "wukan1986/ksrpc"
        type: string
      upstream_branch:
        description: "Upstream branch"
        required: false
        default: "main"
        type: string
      force:
        description: "Force reset target branch to upstream branch"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: write
  actions: read

concurrency:
  group: sync-upstream-main
  cancel-in-progress: true

jobs:
  sync:
    name: Sync Upstream / Fast Forward
    if: ${{ github.ref_name == 'ops/sync' }}
    uses: quantman888/workflow-reusable/.github/workflows/fork-sync.reusable.yml@3a74494444ebf60ec855923fe1f98c111e9a8270
    with:
      target_branch: ${{ github.event.inputs.target_branch || 'main' }}
      upstream_repo: ${{ github.event.inputs.upstream_repo || 'wukan1986/ksrpc' }}
      upstream_branch: ${{ github.event.inputs.upstream_branch || 'main' }}
      force_reset: ${{ github.event.inputs.force == 'true' }}
    secrets:
      RUNNER_PROBE_TOKEN: ${{ secrets.RUNNER_PROBE_TOKEN }}
      GH_APP_ID: ${{ secrets.GH_APP_ID }}
      GH_APP_PRIVATE_KEY: ${{ secrets.GH_APP_PRIVATE_KEY }}
